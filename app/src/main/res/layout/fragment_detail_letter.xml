<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="com.mngs.kimyounghoon.mngs.utils.TimeHelper" />

        <import type="android.view.View" />

        <variable
            name="viewModel"
            type="com.mngs.kimyounghoon.mngs.letterdetail.DetailLetterViewModel" />

        <variable
            name="userActionListener"
            type="com.mngs.kimyounghoon.mngs.LocateListener" />
    </data>

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@{viewModel.title}"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@{viewModel.content}"
            app:layout_constraintTop_toBottomOf="@+id/title" />

        <TextView
            android:id="@+id/date"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@{TimeHelper.getCurrentTimeLocale(viewModel.time)}"
            app:layout_constraintTop_toBottomOf="@+id/content" />

        <Button
            android:id="@+id/answer_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:onClick="@{() -> userActionListener.openAnswer(viewModel.letter) }"
            android:text="답장"
            android:visibility="@{viewModel.isOwnLetter ? View.GONE : View.VISIBLE }"
            app:layout_constraintTop_toBottomOf="@+id/date" />
    </android.support.constraint.ConstraintLayout>
</layout>